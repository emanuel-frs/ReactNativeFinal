import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#14151E',
        padding: 20,
        paddingTop: 30,
        paddingBottom: 0
    }
});